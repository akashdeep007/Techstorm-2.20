import 'package:flutter/material.dart';


class RoboticsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(child: Text('Robotics Page')),
    );
  }
}