import 'package:flutter/material.dart';


class CreativePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(child: Text('CreativePage')),
    );
  }
}